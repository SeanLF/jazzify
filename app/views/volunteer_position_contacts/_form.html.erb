<%= form_for(@volunteer_position_contact, :html => { :role => "form" }) do |f| %>
  <% if @volunteer_position_contact.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@volunteer_position_contact.errors.count, "error") %> prohibited this volunteer_position_contact from being saved:</h4>

      <ul>
      <% @volunteer_position_contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, :class => "control-label" %><br/>
    <%= f.collection_select(:user_id, @elevated_users, :id, :name, class: 'form-control') %>
  </div>
  <div class="form-group">
    <%= f.label :volunteer_position_id, :class => "control-label" %><br/>
    <%= f.collection_select(:volunteer_position_id, @volunteer_positions, :id, :name, class: 'form-control') %>
  </div>
  <div class="form-group">
      <%= f.submit :class => "btn btn-default" %>
  </div>
<% end %>
